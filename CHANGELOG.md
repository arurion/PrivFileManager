# Changelog

このプロジェクトのバージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [5.4.1] - 2026-08-16

### Fixed
- **重大**: 検索→選択→切り取り(コピー)を行うと、検索状態が見えないまま残り続け、
  元に戻せなくなる不具合を修正。選択モードへの出入りで`invalidateOptionsMenu()`が
  発生するたびにSearchViewが折りたたまれた状態で作り直されるが、内部の検索語自体は
  クリアされないままだったため、閉じる手段(×ボタン)が見えなくなっていた。
  検索語が残っている場合は展開状態を復元するようにし、SearchView自体を閉じた際に
  確実に検索語がクリアされるようにした。また、コピー/切り取り/削除などの操作後は
  自動的に検索を終了して通常表示に戻すようにした
- **重大**: 「更新日時」ソートが実質何も起きない(名前順のまま変わらない)不具合を修正。
  `ls -la`の日時欄をパースする際、GNU coreutils形式("Aug 15 12:34")しか
  試しておらず、Android実機の標準ツール(toybox)が使う"yyyy-MM-dd HH:mm"形式
  ("2026-08-15 12:34")を試していなかったため、実機では常にパースに失敗して
  全ファイルの更新日時が0扱いになり、ソートしても実質無効化されていた。
  実機の形式を最優先で試すよう修正し、実際にパースできることも動作確認済み
- ファイル選択の外部呼び出し(GET_CONTENT/PICK/CREATE_DOCUMENT)で、戻るボタンを
  押しても戻れなくなる不具合を修正。ブラウズの起点(`rootPath`)が実際の初期表示位置
  ("/storage/emulated/0")と食い違っており、戻るボタンを押すたびにその起点を
  通り越して延々と上のディレクトリへ遡ろうとし、通常権限では権限不足で
  一覧取得に失敗するディレクトリで詰んでいた。ピッカー系の呼び出しに限り、
  実際の初期表示位置をそのままブラウズの起点として扱うように修正

### Changed
- コピー/切り取り後の通知を、一瞬で消えるToastから、「貼り付け」ボタン付きの
  消えないSnackbarに変更(他の主要なファイルマネージャーと同様の方式)
- 貼り付け先に同名のファイル/フォルダが既にある場合、無言で上書きするのではなく
  "name (1)"のように自動的にリネームして衝突を避けるように変更
- ファイルの長押しメニューから直接「コピー」「切り取り」ができるように対応
  (よく行う操作のため、選択モードを介さずに単一ファイルへ即座に行えるようにした)
- 戻るキーの処理に、AOSP DocumentsUIの実際のソース(`SharedInputHandler.onBack()`)で
  確認した優先順位(ドロワーを閉じる→検索をキャンセル→選択解除→ディレクトリを
  1つ上へ)を適用。以前は「検索をキャンセル」の段階が抜けており、検索中に
  選択操作を行うと検索状態が宙に浮いたまま残る不具合の一因になっていた

## [5.4.0] - 2026-08-16

### Added
- **Shizukuアプリ不要でADB(ワイヤレスデバッグ)権限を取得できるように対応**。
  App Manager(MuntashirAkon)が使っているのと同じ[libadb-android](https://github.com/MuntashirAkon/libadb-android)
  ライブラリを採用し、Android 11+のペアリングコードによるペアリングにも対応
  (証明書はAndroid標準のAndroidKeyStoreで生成)。設定画面から「①ペア設定」
  「②接続」の2段階で設定できる
- **Termux連携**を追加。長押しメニューの「Termuxで開く」、ナビゲーションドロワーの
  「Termuxで開く」から、対象フォルダをTermuxのターミナルで直接開けるようになった
  (公式のRUN_COMMAND Intent方式を利用)
- **外部SAF連携**を追加。SAF(`ACTION_OPEN_DOCUMENT_TREE`)でSDカード等を選択すると、
  実際の絶対パスを推定してブックマークに追加し、以降は通常の特権ファイル操作で
  アクセスできるようにした(content://を経由し続けるのではなく、実パスを特定する
  目的でのみSAFを利用する設計)

### Changed
- **ナビゲーションドロワーを新設**。Amaze File Manager / Material Files / AOSP
  DocumentsUIと同様、クイックアクセス・SAFブックマーク・パス指定移動・
  debuggableアプリ一覧・実行中アプリへジャンプ・Termux連携・設定を、
  増え続けて手狭になっていたオーバーフローメニューから、ハンバーガーアイコン
  (スワイプでも開ける)経由のドロワーへ移設した
- ツールバーのナビゲーションアイコンを、常時ハンバーガー(ドロワー開閉)に統一。
  ディレクトリの階層移動は一覧先頭の「..」行に一本化した(選択モード中のみ
  「選択解除」の×アイコンに切り替わる)
- `MainActivity.kt`内の完全修飾クラス参照(`SafPathResolver`/`ActivityResultContracts`)を
  通常のimportに整理

## [5.3.4] - 2026-08-16

### Fixed
- **重大**: あまりにも大きなテキストファイルを内部エディタで開くと、EditText/TextViewが
  大量の文字列を保持することによるフリーズ・OOMクラッシュが発生する問題を修正。
  Amaze File Managerの実ソース(`ReadTextFileCallable.java`)を確認したところ、
  `MAX_FILE_SIZE_CHARS = 50 * 1024`(50KB)を超える分は最初から読み込まず、
  超過時は保存不可にとどめず**編集そのものを禁止(読み取り専用化)**する設計だった
  (過去の実際のOOMクラッシュ報告Issue #2461への対応)。同じ方針を採用し、
  以前の実装(2MB・保存のみ無効化)から変更した
- 万一サイズの事前チェックをすり抜けた場合の最終防御線として、`OutOfMemoryError`を
  明示的にcatchし、アプリ全体がクラッシュする代わりにエラーメッセージを出して
  画面を閉じるようにした(Amazeも同様の二重防御をしている)

## [5.3.3] - 2026-08-16

### Changed
- ファイル一覧のアイコンタップ判定領域を、Amaze File Managerの実際のソース(`rowlayout.xml`/
  `dimens.xml`)を確認した上で作り直した。前回(5.3.2)は44dp角に拡大したが、実際のAmazeは
  アイコン画像自体(40dp)ではなく、それを囲む**56dp幅×行の全高**のコンテナ全体を
  タップ領域にしていたため、同じ設計(`iconTapArea`)に変更した。合わせて、選択トグル用の
  クリックリスナー/isClickable制御もこの新しいコンテナへ移した
  (前バージョンでFossify File Managerを参照先として挙げていたが、Fossifyにはそもそも
  アイコンタップ選択の機能自体が無く、参照先の選定を誤っていた。訂正してAmazeを参照した)

## [5.3.2] - 2026-08-16

### Fixed
- READMEに「本アプリは通信を外部サーバーへ送信しません(GitHubリポジトリの取り込み機能を
  使った場合を除く)」というGit機能削除前の記述が残っていた。完全にオフラインで動作する旨に修正
- 一覧先頭の「上のディレクトリへ」行で、矢印アイコンを直接タップすると移動が反応しないことが
  あった不具合を修正。原因は、RecyclerViewのビュー使い回しで直前まで通常のファイル行として
  バインドされていた際に`isClickable=true`になっていたアイコンが、`setOnClickListener(null)`
  だけでは`isClickable`が`false`に戻らずタッチを消費してしまい、下の行のクリックへ
  伝播しなくなっていたため。`isClickable`/`isFocusable`も明示的に`false`へ戻すよう修正

### Changed
- ファイル一覧のアイコンタップ判定領域を28dp角から44dp角に拡大(Material Designの推奨最小
  タッチターゲット48dpに近づけた)。視覚上のアイコンサイズは内側パディングで維持しつつ、
  タップ判定だけを広げ、タップ時のリップルフィードバックも追加

## [5.3.1] - 2026-08-16

### Fixed
- **実際のGitHub Actionsビルドで発覚したコンパイルエラーを修正**。5.2.0で選択モードの
  ツールバーをアイコン+メニュー方式に変更した際、`menu_selection.xml`から削除した
  `action_select_all`等のメニュー項目IDへの参照が`MainActivity.kt`側に残ったままになっており、
  `Unresolved reference`エラーでビルドが失敗していた。該当する分岐を削除し、
  再発防止のため全Activityファイル×全リソース種別(string/drawable/layout/menu/
  mipmap/color/id)の参照整合性を機械的に突合検証した
- `setHomeAsUpIndicator(if (...) R.drawable.xxx else null)`が、Int版とDrawable版の
  オーバーロード解決で`Type mismatch: inferred type is Int? but Int was expected`
  エラーになっていた不具合を修正。if/elseで明示的に呼び分けるよう変更

## [5.3.0] - 2026-08-16

### Fixed
- **重大**: 誤った実装だった独自のSAF `DocumentsProvider`を撤去。以前の実装は、システムの
  ファイル選択ダイアログの左メニューに「内部ストレージ」の中身をただ複製表示するだけの、
  実用性の低いものだった。Fossify File Managerの実際のソースコードを確認したところ、
  主要なOSSファイルマネージャーは代わりに**アプリ自身を`ACTION_GET_CONTENT` /
  `ACTION_PICK` / `ACTION_CREATE_DOCUMENT`のハンドラとして登録する**方式を採っており、
  これによりアプリ自身の(Shizuku/Root/run-as対応の)通常のブラウザ画面がそのまま
  「選択モード」として機能する。この方式に置き換えた

### Added
- 他アプリの「ファイルを開く」「保存する」操作で、アプリ選択のチューザーに本アプリが
  表示され、選択すると本アプリの通常のファイルブラウザがそのまま「選択モード」で開くように対応
- 並び替えの基準に「更新日時」「所有者」を追加(以前は名前/サイズ/種類のみだった)

## [5.2.0] - 2026-08-16

### Fixed
- **重大**: 選択モード中でも一覧先頭の「上へ(..)」行やツールバーの「上へ」ボタンが
  selectionModeを無視して常にディレクトリ移動してしまっていた不具合を修正。
  選択内容と表示中ディレクトリが食い違ったまま操作してしまう事故につながる恐れがあったため、
  選択モード中はこれらのタップで移動せず選択モードを解除するだけにした
- 選択を1件も残さず全て解除した場合に、選択モードの表示だけが空虚に残っていた不具合を修正
  (0件になったら自動で選択モードを終了するように)

### Added
- 他アプリの「ファイルを開く/保存する」システムダイアログの左メニューに本アプリが表示されるよう、
  SAF(Storage Access Framework)の`DocumentsProvider`を実装(公開範囲は通常権限でアクセス
  できる外部ストレージのみ)

### Fixed
- **重大**: 非常に大きなテキストファイル/バイナリファイルを開こうとするとメモリ不足でアプリが
  落ちる不具合を修正。特権経路(Shizuku/Root/run-as)はシェル側でbase64化した内容をアプリの
  プロセスメモリを経由して転送する都合上、無制限にファイル全体を読み込んでいたことが原因だった。
  事前にファイルサイズを確認し、閾値を超える場合はテキストエディタは「先頭のみのプレビュー
  (保存不可)」、外部アプリで開く操作は安全に開けない旨を説明するダイアログを表示するように変更
- debuggableアプリのデータブラウズ画面・テキストエディタ・バイナリビューア・設定画面に、
  ツールバー左上の「戻る」ボタンのUIが無かった不具合を修正(物理/ジェスチャーの戻る操作は
  機能していたが、画面上に戻る手段が見えていなかった)
- debuggableアプリのデータブラウズ中など、ルートディレクトリより上へ行けない場面で
  ツールバーの「上へ」ボタンが何も起きない「死んだボタン」になっていた不具合を修正。
  物理・ジェスチャーの「戻る」と同じ挙動(呼び出し元へ戻る)に統一
- 一覧先頭の「上のディレクトリへ」行が、RecyclerViewのビュー使い回しにより、直前まで
  通常のファイル行として表示されていた際のアイコンタップ用リスナーを引き継いでしまい、
  誤って選択状態にできてしまう不具合を修正
- 選択モード中にツールバーの「上へ」ボタンで移動すると、選択状態がクリアされないまま
  ディレクトリだけが切り替わり、選択内容と表示中のディレクトリが食い違う不具合を修正。
  選択を解除した上でそのまま移動するように統一

### Changed
- バイナリビューアに、全体のファイルサイズと「先頭何バイトのみ表示中か」を表示するように改善
- バイナリビューアに「前へ/次へ」ボタンでのページング機能を追加(以前は先頭8KB固定で
  それ以上は確認できなかった)
- テキストエディタに検索機能を追加(文字列検索・次へ/前へ・該当件数表示・該当箇所へ自動スクロール)
- 選択モードのツールバーを、文字ラベルを並べる方式(件数表示と重なって文字が潰れていた)から、
  「削除」アイコン+「その他(⋮)」メニュー方式に変更。全選択・選択を反転・コピー・切り取り・
  圧縮は「その他」メニューにまとめた
- 「選択を反転」機能を追加
- 選択中のファイル/フォルダの表現方法をチェックボックスから行の背景色に変更。以前は選択モードの
  ON/OFFでチェックボックスの表示/非表示が切り替わり、一覧全体の横幅が変化してガタつく問題があった
- 選択モード中のツールバー表示を、長い「5件選択中」という文言(スペースが狭く文字が潰れていた)
  から短い「5件」表記+「閉じる(×)」アイコンに変更

## [5.1.0] - 2026-08-16

### Added
- アーカイブの圧縮/展開に対応(ZIP / TAR / TAR.GZ / TAR.BZ2 / TAR.XZ / 7Z(展開のみ) / RAR(展開のみ))。
  Apache Commons Compress(Apache-2.0)・junrar(UnRARライセンス、展開専用)による純Java実装で、
  Android NDKのネイティブビルドは不要
- ファイル/フォルダの長押しメニューから直接圧縮できるように対応(単一選択でも複数選択でも可能)
- ツールバー左上の「戻る」ボタンと、一覧最上部の「.. (上へ)」項目で親ディレクトリへ移動できるように対応
- 拡張子に応じたファイル種別アイコン(画像/動画/音声/アーカイブ/PDF/APK/コード/文書)を追加。
  AOSP DocumentsUIのIconUtilsと同様、カテゴリ単位でベクターアイコンを出し分ける方式
- 設定画面に「隠しファイルを表示」「削除前に確認ダイアログを表示」を追加
- アプリ設定(シェルエンジンの選択・隠しファイル表示・削除確認・並び替え)を`SharedPreferences`で
  永続化するように対応
- 新規フォルダ/ファイル作成をオーバーフローメニューの奥からFAB(フローティングアクションボタン)に
  昇格。ファイル操作で最も頻繁に使う操作の一つのため、常に手の届く位置に配置
- debuggableアプリ一覧に実際のアプリアイコンを表示(パッケージ名だけの一覧から視認性を改善)
- タップ/長押しの役割を整理: 以前はタップ時に拡張子から自動判定して問答無用でアプリを開いていたが、
  判定ミス時の違和感を無くすため常に「開き方」を確認するメニューに変更(旧・長押しメニューの一項目を
  タップ側へ移設)。長押しは削除・リネーム・圧縮・展開・パーミッション変更など、より詳しい操作専用に整理
- アイコン部分をタップすると(多くのファイルマネージャーと同様)選択モードのトグルとして機能するように対応
- Fossify File Managerを参考に「ファイル形式を指定して開く」機能を追加(拡張子によらずテキスト/画像/
  動画/音声/PDF/APK/汎用のいずれかとして強制的に開ける)
- 操作メニューをダイアログ/ボトムシート(下からせり出す形式)のどちらで表示するか設定で選べるように対応
  (デフォルトはボトムシート)
- 他のOSSファイルマネージャー(Amaze / Fossify等)と同様、SAF(Storage Access Framework)の
  DocumentsProviderを実装。他アプリの「ファイルを開く/保存する」システムダイアログの左メニューに
  本アプリが表示され、そこから直接ファイルを選択できるようになった(公開範囲は通常権限でアクセス
  できる外部ストレージのみ。Shizuku/Root越しの特権領域はDocumentsProviderの同期API設計とは
  相性が悪いため対象外)

### Fixed
- debuggableアプリのデータブラウズ中など、ルートディレクトリより上へ行けない場面で
  ツールバーの「上へ」ボタンが何も起きない「死んだボタン」になっていた不具合を修正。
  物理・ジェスチャーの「戻る」と同じ挙動(呼び出し元へ戻る)に統一

### Fixed
- シェルエンジンの優先モード設定がアプリ再起動のたびに「自動」へ戻ってしまっていた不具合を修正
  (メモリ上の変数のみで保存されていなかったため)

### Removed
- GitHubリポジトリのclone/pull機能(JGit)を削除。diff/commit/push/ブランチ切替のない
  単機能(cloneのみ)で実用性が乏しかったため撤去。これに伴い`INTERNET`/
  `ACCESS_NETWORK_STATE`権限も不要になったため削除(本アプリは完全にオフラインで動作します)

### Changed
- README/NOTICEを現状の実装に合わせて全面更新
- ProGuardルールを整理(JGit関連ルールを削除し、commons-compress/junrar/xz向けのdontwarnを追加)

## [5.0.0] - 2026-08-12

- Shizuku / Root / 通常権限を自動判別して切り替えるファイル操作(閲覧・複数選択・コピー/切り取り・
  削除・リネーム・新規作成・chmod・並び替え・検索・パンくずナビゲーション)
- debuggableアプリの`/data/data/<package>`配下を`run-as`経由で閲覧・編集
- 外部アプリとの連携(開くアプリを選択 → 編集内容を検知して書き戻し)
- GitHubリポジトリのclone/pull(JGit) ※5.1.0で削除
- ZIP形式の圧縮/展開 ※5.1.0でTAR系/7Z/RARにも拡張
