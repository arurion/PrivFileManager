# Changelog

このプロジェクトのバージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [5.1.0] - 2026-08-16

### Added
- アーカイブの圧縮/展開に対応(ZIP / TAR / TAR.GZ / TAR.BZ2 / TAR.XZ / 7Z(展開のみ) / RAR(展開のみ))。
  Apache Commons Compress(Apache-2.0)・junrar(UnRARライセンス、展開専用)による純Java実装で、
  Android NDKのネイティブビルドは不要
- ファイル/フォルダの長押しメニューから直接圧縮できるように対応(単一選択でも複数選択でも可能)
- ツールバー左上の「戻る」ボタンと、一覧最上部の「.. (上へ)」項目で親ディレクトリへ移動できるように対応
- 拡張子に応じたファイル種別アイコン(画像/動画/音声/アーカイブ/PDF/APK/コード/文書)を追加。
  AOSP DocumentsUIのIconUtilsと同様、カテゴリ単位でベクターアイコンを出し分ける方式
- 設定画面に「隠しファイルを表示」「削除前に確認ダイアログを表示」を追加
- アプリ設定(シェルエンジンの選択・隠しファイル表示・削除確認・並び替え)を`SharedPreferences`で
  永続化するように対応
- 新規フォルダ/ファイル作成をオーバーフローメニューの奥からFAB(フローティングアクションボタン)に
  昇格。ファイル操作で最も頻繁に使う操作の一つのため、常に手の届く位置に配置
- debuggableアプリ一覧に実際のアプリアイコンを表示(パッケージ名だけの一覧から視認性を改善)

### Fixed
- シェルエンジンの優先モード設定がアプリ再起動のたびに「自動」へ戻ってしまっていた不具合を修正
  (メモリ上の変数のみで保存されていなかったため)

### Removed
- GitHubリポジトリのclone/pull機能(JGit)を削除。diff/commit/push/ブランチ切替のない
  単機能(cloneのみ)で実用性が乏しかったため撤去。これに伴い`INTERNET`/
  `ACCESS_NETWORK_STATE`権限も不要になったため削除(本アプリは完全にオフラインで動作します)

### Changed
- README/NOTICEを現状の実装に合わせて全面更新
- ProGuardルールを整理(JGit関連ルールを削除し、commons-compress/junrar/xz向けのdontwarnを追加)

## [5.0.0] - 2026-08-12

- Shizuku / Root / 通常権限を自動判別して切り替えるファイル操作(閲覧・複数選択・コピー/切り取り・
  削除・リネーム・新規作成・chmod・並び替え・検索・パンくずナビゲーション)
- debuggableアプリの`/data/data/<package>`配下を`run-as`経由で閲覧・編集
- 外部アプリとの連携(開くアプリを選択 → 編集内容を検知して書き戻し)
- GitHubリポジトリのclone/pull(JGit) ※5.1.0で削除
- ZIP形式の圧縮/展開 ※5.1.0でTAR系/7Z/RARにも拡張
